# Foundry 部署（复制为 .env，不要提交 .env）
SEPOLIA_RPC_URL=https://ethereum-sepolia-rpc.publicnode.com
# PRIVATE_KEY=...
# ETHERSCAN_API_KEY=...

# 全新 ZZTKA/ZZTKB：不要设置 TOKEN_A / TOKEN_B，使用：
#   make deploy-fresh-tokens NETWORK=sepolia
# 部署后把日志中的 Token / Staking 地址写入 frontend/.env.local
