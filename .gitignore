# Compiler / Foundry artifacts
cache/
out/
broadcast/

# Docs and local notes

md/
xmind/

# forge install output (submodule lib/forge-std remains tracked as a gitlink)
lib/

# Env and package manager caches
.env
.env.*
!.env.example
# pnpm store when store-dir lives inside the repo (root + nested workspaces)
/.pnpm-store/
**/.pnpm-store/

.gas-snapshot

# Frontend build and test artifacts (see also frontend/.gitignore)
frontend/node_modules/
frontend/.next/
frontend/test-results/
frontend/tests/

src/DualPoolStakingAdminOld.sol
src/DualPoolStakingOld.sol

script/acceptance.sh
script/sync_frontend_addresses.py

